# API base path — relative for dev (uses Vite proxy), full URL for production
VITE_API_BASE=/api

# WebSocket URL — leave empty to auto-derive from current host + /ws path
# VITE_WS_URL=wss://api.yourdomain.com/ws
